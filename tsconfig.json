{
  "compilerOptions": {
    "declaration": true, // Generate .d.ts declaration files
    "emitDeclarationOnly": true, // Only emit .d.ts files (useful for separating builds)
    "outDir": "./dist", // Specify output directory for the declarations
  }
}
